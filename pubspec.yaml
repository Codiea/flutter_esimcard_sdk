name: esimcardsdk
description: ESIMCARD SDK for Flutter enables seamless integration of ESIMCARD services into apps. Access APIs for robust interactions, enhancing Flutter capabilities.
version: 0.0.7
homepage: https://codiea.io/
repository: https://github.com/Codiea/flutter_esimcard_sdk

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  dio: ^5.1.0  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0


flutter:
